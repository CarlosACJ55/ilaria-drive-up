import XCTest
@testable import ilaria_drive_up

final class ilaria_drive_upTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ilaria_drive_up().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
